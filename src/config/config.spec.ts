// import Joi from 'joi';
// import { CONFIG_DB_SCHEMA } from '../config/config.types';
// import { log } from 'console';

// function expectValidate(schema: Joi.Schema, value: any) {
//   return expect(schema.validate(value, { abortEarly: false }).error.message);
// }

// describe('Schema Unit Tests', () => {
//   it('DB Schema', () => {
//     const schema = Joi.object({
//       ...CONFIG_DB_SCHEMA,
//     });

//     log(schema);

//     describe(`DB_PROVIDER`, () => {
//       test(`invalid cases`, () => {
//         expectValidate(schema, {}).toContain('"DB_PROVIDER is required');
//       });

//       // test(`valid cases`, () => {
//       //   const arrange = [`sqlite`, `postgresql`];

//       //   arrange.forEach((value) => {
//       //     expectValidate(schema, { DB_PROVIDER: value }).not.toContain(
//       //       `DB_PROVIDER`,
//       //     );
//       //   });
//       // });
//     });
//   });
//   // it('ConfigModule Unit Tests', () => {});
// });

describe(``, () => {
  test(``, () => console.log(`emoty`));
});
